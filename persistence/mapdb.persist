Strategies {
	default = everyChange
}
Items {
	* : strategy = everyUpdate, restoreOnStartup
}

// this saves every item's last value and restores on reboot, thus not having null/undefined in the beginning